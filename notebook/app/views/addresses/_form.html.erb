<%= form_for(@address, html: {class: "basic-grey"}) do |f| %>
  <% if @address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
      <% @address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1>Endereços</h1>

  <div class="field">
    <label>
      <span>
        <%= f.label :street %><br>
      </span>
    </label>
    <%= f.text_field :street %>
  </div>
  <div class="field">
    <label>
      <span>
        <%= f.label :city %><br>
      </span>
    </label>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <label>
      <span>
        <%= f.label :state %><br>
      </span>
    </label>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <label>
      <span>
        <%= f.label :contact_id %><br>
      </span>
    </label>
    <%= f.text_field :contact_id %>
  </div>
  <div class="actions">
    <div class="left-margin">
      <%= f.submit "Enviar", class: "button" %>
      <%= link_to 'Back', addresses_path, class: "button-back" %>
    </div>
  </div>
<% end %>
